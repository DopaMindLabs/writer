{
  "name": "writer-sync",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Provider-, transport- and pairing-agnostic sync engine: contracts, operation protocol and encryption seams.",
  "exports": {
    "./core": "./src/core/index.ts",
    "./crypto": "./src/crypto/index.ts",
    "./operations": "./src/operations/index.ts",
    "./pairing": "./src/pairing/index.ts",
    "./providers/webrtc": "./src/providers/webrtc/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "TZ=UTC vitest run --root ."
  }
}
